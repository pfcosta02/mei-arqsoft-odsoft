---
# HPA para Blue Deployment
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: lms-authnusers-command-scaler-blue
  namespace: {{NAMESPACE}}
  labels:
    app: lms-authnusers-command
    color: blue
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: lms-authnusers-command-blue
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
---
# HPA para Green Deployment
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: lms-authnusers-command-scaler-green
  namespace: {{NAMESPACE}}
  labels:
    app: lms-authnusers-command
    color: green
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: lms-authnusers-command-green
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 50
