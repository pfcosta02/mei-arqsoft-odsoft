
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lms-authnusers-query-{{COLOR}}
  namespace: dev
  labels:
    app: lms-authnusers-query
    color: {{COLOR}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lms-authnusers-query
      color: {{COLOR}}
  template:
    metadata:
      labels:
        app: lms-authnusers-query
        color: {{COLOR}}
        version: {{COLOR}}
    spec:
      containers:
      - name: lms-authnusers-query
        image: {{IMAGE}}
        ports:
        - containerPort: 8082
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
