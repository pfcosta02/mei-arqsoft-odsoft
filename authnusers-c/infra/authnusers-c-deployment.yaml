apiVersion: apps/v1
kind: Deployment
metadata:
  name: authnusers-c-{{COLOR}}
  namespace: dev
  labels:
    app: authnusers-c
    color: {{COLOR}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: authnusers-c
      color: {{COLOR}}
  template:
    metadata:
      labels:
        app: authnusers-c
        color: {{COLOR}}
        version: {{COLOR}}
    spec:
      containers:
      - name: authnusers-c
        image: {{IMAGE}}
        ports:
        - containerPort: 8089
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
