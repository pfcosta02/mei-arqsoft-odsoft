apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookgenre-c-{{COLOR}}
  namespace: dev
  labels:
    app: bookgenre-c
    color: {{COLOR}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bookgenre-c
      color: {{COLOR}}
  template:
    metadata:
      labels:
        app: bookgenre-c
        color: {{COLOR}}
        version: {{COLOR}}
    spec:
      containers:
      - name: bookgenre-c
        image: {{IMAGE}}
        ports:
        - containerPort: 8083
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
