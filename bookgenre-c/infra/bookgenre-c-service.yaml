apiVersion: v1
kind: Service
metadata:
  name: bookgenre-c-service
  namespace: dev
  labels:
    app: bookgenre-c
spec:
  type: LoadBalancer
  selector:
    app: bookgenre-c
    color: blue  # Inicialmente aponta para blue. Será alterado via kubectl patch durante o deployment
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  sessionAffinity: ClientIP  # Mantém sessões durante o switch

