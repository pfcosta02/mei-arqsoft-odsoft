apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookgenre-q-{{COLOR}}
  namespace: dev
  labels:
    app: bookgenre-q
    color: {{COLOR}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: bookgenre-q
      color: {{COLOR}}
  template:
    metadata:
      labels:
        app: bookgenre-q
        color: {{COLOR}}
        version: {{COLOR}}
    spec:
      containers:
      - name: bookgenre-q
        image: {{IMAGE}}
        ports:
        - containerPort: 8084
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
