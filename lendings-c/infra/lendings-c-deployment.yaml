
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lendings-c-{{COLOR}}
  namespace: dev
  labels:
    app: lendings-c
    color: {{COLOR}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lendings-c
      color: {{COLOR}}
  template:
    metadata:
      labels:
        app: lendings-c
        color: {{COLOR}}
        version: {{COLOR}}
    spec:
      containers:
      - name: lendings-c
        image: {{IMAGE}}
        ports:
        - containerPort: 8881
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
