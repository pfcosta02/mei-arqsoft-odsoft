
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lendings-q-{{COLOR}}
  namespace: dev
  labels:
    app: lendings-q
    color: {{COLOR}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: lendings-q
      color: {{COLOR}}
  template:
    metadata:
      labels:
        app: lendings-q
        color: {{COLOR}}
        version: {{COLOR}}
    spec:
      containers:
      - name: lendings-q
        image: {{IMAGE}}
        ports:
        - containerPort: 8882
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
