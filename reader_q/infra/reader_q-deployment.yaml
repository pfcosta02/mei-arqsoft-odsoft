
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reader_q-{{COLOR}}
  namespace: dev
  labels:
    app: reader_q
    color: {{COLOR}}
spec:
  replicas: 2
  selector:
    matchLabels:
      app: reader_q
      color: {{COLOR}}
  template:
    metadata:
      labels:
        app: reader_q
        color: {{COLOR}}
        version: {{COLOR}}
    spec:
      containers:
      - name: reader_q
        image: {{IMAGE}}
        ports:
        - containerPort: 8082
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
